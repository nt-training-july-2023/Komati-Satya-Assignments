package Variables;

public class Naming_Convention {
	public static void main(String[] args) {
	int number;
	int Number; //Both are not same variables are case sensitive
	String $name; //Variables may start with $ 
	//float if;  it gives an error because if is a keyword
	byte Naming_Convention=10;
	System.out.print(Naming_Convention);
	long abcdefh; //no limit on length of variable
	}
}
